{{#indent}}
if (method_exists($this->I, __FUNCTION__)) {{#curly}}{{#indent}}
$this->I->{{ camelize_lower rendered_children.name }}({{> argument_passthrough}});
return;
{{/indent}}
{{/curly}}
{{#comment '//'}}{{#if has_description?}}{{{ rendered_children.description }}}
{{/if}}{{#if has_tags?}}Tags: {{{ join rendered_children.tags ' '}}}
{{/if}}{{/comment}}{{#each rendered_children.body as |step|}}
{{{step}}}{{/each}}
{{#unless has_call?}}throw new IncompleteTestError('Action `{{ camelize_lower rendered_children.name }}` is not defined. Implement `public function {{ camelize_lower rendered_children.name }}({{> argument_passthrough}})` in `{{> actor_class }}`.');
{{/unless}}
{{/indent}}